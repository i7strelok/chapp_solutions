{% extends 'base.html.twig' %}

{% block title %}Etiqueta{% endblock %}

{% block body %}
    <h1>Etiqueta</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etiquetum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ etiquetum.nombre }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ etiquetum.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etiqueta_index') }}">back to list</a>

    <a href="{{ path('app_etiqueta_edit', {'id': etiquetum.id}) }}">edit</a>

    {{ include('etiqueta/_delete_form.html.twig') }}
{% endblock %}
