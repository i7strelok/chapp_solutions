<form method="post" action="{{ path('app_etiqueta_delete', {'id': etiquetum.id}) }}" onsubmit="return confirm('¿Seguro que desea eliminar esta etiqueta?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etiquetum.id) }}">
    <button class="btn">Delete</button>
</form>
