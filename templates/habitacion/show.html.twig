{% extends 'base.html.twig' %}

{% block title %}Habitacion{% endblock %}

{% block body %}
    <h1>Habitacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ habitacion.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ habitacion.numero }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ habitacion.descripcion }}</td>
            </tr>
            <tr>
                <th>Capacidad</th>
                <td>{{ habitacion.capacidad }}</td>
            </tr>
            <tr>
                <th>Precio_diario</th>
                <td>{{ habitacion.precioDiario }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_habitacion_index') }}">back to list</a>

    <a href="{{ path('app_habitacion_edit', {'id': habitacion.id}) }}">edit</a>

    {{ include('habitacion/_delete_form.html.twig') }}
{% endblock %}
